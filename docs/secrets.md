# Secrets Inventory

## Purpose

This document records where runtime secrets are defined, how they are injected, and whether their values are stored in Terraform state.

## Inventory

| Secret | Cloud | Terraform location | Runtime consumer | Value in Terraform state? | Current injection method | Recommended owner |
|---|---|---|---|---|---|---|
| Database password | GCP | `infra/terraform/modules/secret-manager` | Cloud Run API | Yes, generated by Terraform and written as a Secret Manager version | Secret Manager reference in Cloud Run env | Cloud platform owner |
| JWT / application secret | GCP | `infra/terraform/modules/secret-manager` | Cloud Run API | No value managed by Terraform; only the secret container is created | Secret Manager reference in Cloud Run env | Application platform owner |
| Gemini API key | GCP | `infra/terraform/modules/secret-manager` | Cloud Run API | No value managed by Terraform; only the secret container is created | Secret Manager reference in Cloud Run env | LLM platform owner |
| OpenRouter API key | GCP | `infra/terraform/modules/secret-manager` | Future/runtime LLM integrations | No value managed by Terraform; only the secret container is created | Manual or protected CI population in Secret Manager | LLM platform owner |
| DeepSeek API key | GCP | `infra/terraform/modules/secret-manager` | Future/runtime LLM integrations | No value managed by Terraform; only the secret container is created | Manual or protected CI population in Secret Manager | LLM platform owner |
| Google OAuth client ID | GCP | `infra/terraform/modules/secret-manager` | Auth runtime when OAuth is enabled | No value managed by Terraform; only the secret container is created | Manual or protected CI population in Secret Manager | Identity owner |
| Google OAuth client secret | GCP | `infra/terraform/modules/secret-manager` | Auth runtime when OAuth is enabled | No value managed by Terraform; only the secret container is created | Manual or protected CI population in Secret Manager | Identity owner |
| Database password | AWS | `infra/terraform/modules/aws/secrets-manager` | RDS and ECS API/worker database URL | Yes, generated by Terraform and stored as a Secrets Manager version | Secrets Manager secret and ECS task secret/env wiring | Cloud platform owner |
| JWT secret key | AWS | `infra/terraform/modules/aws/secrets-manager` | ECS API/worker | No value managed by Terraform; only the secret container is created | Secrets Manager ARN injected into ECS task definitions | Application platform owner |
| Gemini API key | AWS | `infra/terraform/modules/aws/secrets-manager` | ECS API/worker when configured | No value managed by Terraform; only the secret container is created | Secrets Manager ARN injected into ECS task definitions when used | LLM platform owner |
| DeepSeek API key | AWS | `infra/terraform/modules/aws/secrets-manager` | ECS API/worker | No value managed by Terraform; only the secret container is created | Secrets Manager ARN injected into ECS task definitions | LLM platform owner |
| OpenRouter API key | AWS | `infra/terraform/modules/aws/secrets-manager` | ECS API/worker when configured | No value managed by Terraform; only the secret container is created | Secrets Manager ARN injected into ECS task definitions when used | LLM platform owner |
| Google OAuth client ID / secret | AWS | `infra/terraform/modules/aws/secrets-manager` | Auth runtime when OAuth is enabled | No value managed by Terraform; only the secret containers are created | Secrets Manager ARN injection when wired by runtime | Identity owner |
| SMTP / Resend API key | AWS | `infra/terraform/modules/aws/secrets-manager` | ECS API/worker email sending | No value managed by Terraform; only the secret container is created | Secrets Manager ARN injected into ECS task definitions | Application platform owner |
| S3 access key / secret key | AWS | `infra/terraform/modules/aws/secrets-manager` | Runtime storage fallback if static keys are ever used | No value managed by Terraform; only the secret containers are created | Prefer ECS task role; use Secrets Manager only for legacy fallback | Cloud platform owner |
| Test CI password | AWS | `infra/terraform/modules/aws/secrets-manager` | CI/smoke-test account flows | No value managed by Terraform; only the secret container is created | Protected CI or manual population in Secrets Manager | Release owner |
| PostgreSQL administrator password | Azure | `infra/terraform/live/azure/dev`, `infra/terraform/modules/azure/postgres`, `infra/terraform/modules/azure/key-vault`, `infra/terraform/modules/azure/container-apps` | Azure PostgreSQL and Container Apps API/worker database URL | Yes, supplied as a sensitive Terraform variable and written to Key Vault/Container Apps secret state | Key Vault secret plus Container Apps secret | Cloud platform owner |
| Azure OpenAI API key | Azure | `infra/terraform/live/azure/dev`, `infra/terraform/modules/azure/key-vault`, `infra/terraform/modules/azure/container-apps` | Container Apps API/worker | Yes, when provided through Terraform | Key Vault secret plus Container Apps secret | LLM platform owner |
| JWT secret | Azure | `infra/terraform/live/azure/dev`, `infra/terraform/modules/azure/key-vault`, `infra/terraform/modules/azure/container-apps` | Container Apps API/worker | Yes, when provided through Terraform | Key Vault secret plus Container Apps secret | Application platform owner |
| Application secret key | Azure | `infra/terraform/live/azure/dev`, `infra/terraform/modules/azure/key-vault`, `infra/terraform/modules/azure/container-apps` | Container Apps API/worker | Yes, when provided through Terraform | Key Vault secret plus Container Apps secret | Application platform owner |
| Resend API key | Azure | `infra/terraform/live/azure/dev`, `infra/terraform/modules/azure/key-vault`, `infra/terraform/modules/azure/container-apps` | Container Apps API/worker email sending | Yes, when provided through Terraform | Key Vault secret plus Container Apps secret | Application platform owner |
| Azure Storage connection string | Azure | `infra/terraform/modules/azure/storage`, `infra/terraform/modules/azure/container-apps` | Container Apps API/worker object storage | Yes, generated by Azure and stored in Terraform state as a sensitive output/Container Apps secret | Container Apps secret | Cloud platform owner |
| Redis primary key / URL | Azure | `infra/terraform/modules/azure/redis`, `infra/terraform/modules/azure/container-apps` | Container Apps API/worker cache | Yes, generated by Azure and stored in Terraform state as a sensitive output/Container Apps secret | Container Apps secret | Cloud platform owner |

## Handling Rules

- Terraform may create secret containers and IAM bindings.
- Prefer injecting secret values through cloud secret managers or protected CI operations.
- Do not commit `.tfvars`, `.env`, generated state, or secret values.
- Treat Terraform state as sensitive.
- GCP still uses project-level `roles/iam.securityAdmin` and `roles/secretmanager.admin` for Terraform because it manages IAM bindings, Workload Identity Federation, and Secret Manager resources. Scope these down in a dedicated IAM migration after validating resource-level bindings.
