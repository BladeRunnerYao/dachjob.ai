# ------------------------------------------------------------------------------
# Terraform Backend Config – AWS S3
# ------------------------------------------------------------------------------
# The S3 bucket for state must be created before first terraform init.
#
# Bootstrap (one-time, manually or via script):
#   aws s3api create-bucket \
#     --bucket dachjob-dev-terraform-state \
#     --region eu-west-1 \
#     --create-bucket-configuration LocationConstraint=eu-west-1
#   aws s3api put-bucket-versioning \
#     --bucket dachjob-dev-terraform-state \
#     --versioning-configuration Status=Enabled
#   aws s3api put-bucket-encryption \
#     --bucket dachjob-dev-terraform-state \
#     --server-side-encryption-configuration '{"Rules":[{"ApplyServerSideEncryptionByDefault":{"SSEAlgorithm":"AES256"}}]}'
#   aws s3api put-public-access-block \
#     --bucket dachjob-dev-terraform-state \
#     --public-access-block-configuration BlockPublicAcls=true,IgnorePublicAcls=true,BlockPublicPolicy=true,RestrictPublicBuckets=true
#   aws dynamodb create-table \
#     --table-name dachjob-dev-terraform-lock \
#     --attribute-definitions AttributeName=LockID,AttributeType=S \
#     --key-schema AttributeName=LockID,KeyType=HASH \
#     --billing-mode PAY_PER_REQUEST \
#     --region eu-west-1
#
# Then init with:
#   terraform init -backend-config=backend.conf

bucket         = "dachjob-dev-terraform-state"
key            = "aws/dev/terraform.tfstate"
region         = "eu-west-1"
encrypt        = true
dynamodb_table = "dachjob-dev-terraform-lock"
