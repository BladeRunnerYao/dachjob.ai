# GCP Dev Terraform Root

This root deploys the GCP dev environment. The GCP deploy workflow uses this directory via `GCP_TF_DIR=infra/terraform/live/gcp/dev`.

Use `terraform.tfvars.example` as a starting point for local planning. Do not commit real `.tfvars` files.
