# GCS backend configuration for the dev environment.
# Copy this file to backend.conf and update the bucket name after
# running `terraform apply` once (or create the bucket manually).
#
# bucket = "dachjob-dev-terraform-state"
# prefix = "dev"
